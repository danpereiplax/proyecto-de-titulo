{
  "compilerOptions": {
    "baseUrl": ".",                      // base es la raíz del repo
    "paths": {
      "@/*": ["frontend/src/*"]          // path correcto hacia la carpeta src dentro de frontend
    }
  },
  "exclude": ["node_modules", "dist"]
}
